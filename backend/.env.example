JWT_SECRET=secret
DB_NAME=db_name
DB_USER=user
DB_PASSWORD=password
DB_HOST=localhost
PORT=8080
ENV=local

# Optional: Full database connection string (overrides individual DB_* vars if set)
# DATABASE_URL=postgres://user:password@host:5432/dbname?sslmode=disable

# Test database configuration
TEST_DB_NAME=goalify_test
TEST_DB_PASSWORD=goalify
TEST_DB_USER=goalify
TEST_DB_HOST=localhost

# Optional: Full test database connection string (overrides individual TEST_DB_* vars if set)
# TEST_DB_CONN_STRING=postgres://user:password@host:5432/testdb?sslmode=disable
